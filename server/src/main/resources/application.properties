# api listens on 8787, nginx proxies /api to it in prod
server.port=${PORT:8787}
# read the real client ip from the x-forwarded-for nginx sets
server.forward-headers-strategy=native
# don't serialise null fields, so an anonymous note has no author key
spring.jackson.default-property-inclusion=non_null
spring.main.banner-mode=off
